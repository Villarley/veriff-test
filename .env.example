# Veriff API Configuration
# Get your API key from: https://station.veriff.com/
VERIFF_API_KEY=your_veriff_api_key_here

# Veriff Base URL (usually doesn't need to be changed)
VERIFF_BASE_URL=https://stationapi.veriff.com

# Callback URL for webhooks (HTTPS required by Veriff)
# For local development: Leave empty or omit (automatically skipped for HTTP)
# For production: Set to your HTTPS webhook URL
# VERIFF_CALLBACK_URL=https://yourdomain.com/api/veriff/webhook
